var Each = module.exports = {};

Each.fn = function(content, array, key, index, animate)
{
  // Actual browser side code for mc:each tag
}

Each.signature = {
  fn: '__MC__.each', // maybe
  args: ['array', 'key', 'index', 'animate']
}
