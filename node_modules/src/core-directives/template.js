var Template = module.exports = {};

Template.fn = function(content, context)
{
  // Actual browser side code for mc:template tag
}

Template.signature = {
  fn: '__MC__.template', // maybe
  args: ['context']
};
