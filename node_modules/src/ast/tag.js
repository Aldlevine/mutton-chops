module.exports = Tag;

var Node = require('src/ast/node')
;

/**
 * A DOM tag
 * @class Tag
 * @extends Node
 * @param  {Object} opts - The tag options
 */
var Tag = function(opts)
{
  if(!(this instanceof Tag))
    return new Tag(opts);

  Node.call(this, opts);
}

Tag.prototype = Object.create(Node.prototype);
