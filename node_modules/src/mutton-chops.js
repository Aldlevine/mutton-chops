module.exports = MuttonChops;

var MCUtils = require('src/mc-utils')
;

/**
 * @class MuttonChops
 * @param {Object|Function} opts
 */
function MuttonChops(opts)
{
  if(!(this instanceof MuttonChops)))
    return new MuttonChops(opts);
    
  var opts = MCUtils.defaults({}, opts);
}
