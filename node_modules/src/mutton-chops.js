var Utils = require('src/utils')
;

/**
 * @class MuttonChops
 * @param {Object|Function} opts
 */
var MuttonChops = module.exports = function MuttonChops(opts)
{
  if(!(this instanceof MuttonChops))
    return new MuttonChops(opts);

  var opts = Utils.defaults({}, opts);
}
